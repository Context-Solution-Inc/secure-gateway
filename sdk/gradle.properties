# AndroidX is required by :android-aar's transitive deps (lazysodium-android → androidx.core).
# It only affects the Android library module; the JVM modules ignore it.
android.useAndroidX=true
android.nonTransitiveRClass=true
